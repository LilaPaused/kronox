# Future AI Layer

The AI layer is not responsible for calculations.

Its role:
1. Interpret user messages (e.g. "I worked one extra hour")
2. Propose structured data
3. Ask for clarification if ambiguous
4. Pass validated data to Python

The AI never writes directly to Google Sheets.
